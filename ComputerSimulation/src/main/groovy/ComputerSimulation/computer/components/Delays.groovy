package ComputerSimulation.computer.components

trait Delays {

    // tick every second
    int CLOCK = 1000

    // simplify simulation as ignoring delays

    int NOT_delay = 0
    int AND_delay = 0
    int OR_delay = 0
}
